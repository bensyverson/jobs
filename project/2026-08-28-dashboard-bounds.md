# Dashboard bounds: range selector and chip caps

*2026-08-28. Filed as root ffoTg. Discussed with Ben: the Actors board and the Log chip strips are unbounded in time (Hirewell shows ~400 actor chips); a shared `?range=` selector, default 7D, bounds both, and the single-agent page renders its events through the shared log row, resolving XtNhh. Alternatives considered: a fixed 7-day default with no selector (too rigid), and reusing the history scrubber window (answers a different question).*

Leaves as imported (root `ffoTg`): `4HZ7D` range selector on the Actors board (7D · 14D · 30D · All, `?range=`, default 7D; only actors with an event in range get a column); `7QB2y` the same selector on the Log plus actor/label chip strips bounded to the range, most-recent first, capped at 24 with a "+N more" expander (blocked on `4HZ7D` for the shared helper); `yOO7t` the single-agent page's Events section renders through the shared `log-row` block and `log-row.mjs` (resolves `XtNhh`); `02FhN` a 24H · 7D · 30D window control on the single-agent timeline (`?window=`, separate from `?range=` because it answers a different question and defaults differently). Full descriptions and criteria: `job show <id>`.
